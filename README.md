# ym-audit
Audit-first experiment suite + Overleaf export pipeline (Colab → flat ZIP) for 4D SU(N) Yang–Mills companion-paper programme. Runs tests, writes results.json, and generates LaTeX tables for reproducibility.
